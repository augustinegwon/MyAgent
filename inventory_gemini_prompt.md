# 작업지시서 — 구글 스프레드시트 물품 재고 관리 시스템 (Google Apps Script)

당신은 구글 스프레드시트와 Google Apps Script(GAS)에 능숙한 시니어 개발자입니다.
아래 **제품 요구사항 정의서(PRD)** 를 그대로 구현할 수 있는 **완성된 스프레드시트 구조와 Apps Script 코드**를 만들어 주세요.
출력은 (1) 각 시트의 구조(열 정의·수식·데이터 검증 규칙), (2) 전체 `.gs` 코드, (3) 최초 셋업 방법 순서로 정리해 주세요.

---

## 1. 시스템 개요

회사 물품의 재고를 **위치(창고/오피스)별·투어 패키지별**로 관리하는 스프레드시트입니다.
핵심 기능은 두 가지입니다.

1. **재고 현황 보기** — 모든 물품의 위치별 재고 수량을 한 화면에서 확인.
2. **재고 편집** — 물품별로 위치·수량을 구매/이동/폐기 이벤트로 편집.

### 설계 원칙 (반드시 준수)

- **보기 시트의 수량은 전부 스프레드시트 함수(SUMIFS 등)로 계산**한다. Apps Script로 셀 값을 직접 쓰지 않는다. (Apps Script 반영 지연을 피하기 위함)
- **편집(구매/이동/폐기)은 Apps Script가 "거래원장(Ledger) 시트"에 한 줄(row)을 append** 하는 방식으로만 처리한다. 원장에 기록되면 보기 시트의 SUMIFS가 자동으로 갱신된다.
- 물품명·카테고리·위치·상태 등 **모든 마스터 데이터는 `Settings` 시트에서만 관리**하고, 다른 시트와 스크립트는 이 값을 참조(드롭다운·named range)한다. 하드코딩 금지.

---

## 2. 도메인 규칙

### 2-1. 물품 자산 상태 (3분류)

| 구분 | 의미 | 시스템 표현 |
|------|------|-------------|
| ① 회사 자산 아님 | 구매 전 또는 폐기 후 | 원장에 기록 없음 / 재고에서 빠져 있음 |
| ② 회사 자산 · 투어 미투입 | 창고·오피스에 보관 중인 재고 | 물리 위치 버킷에 소속 |
| ③ 회사 자산 · 투어 투입 | 특정 투어 패키지에 배정됨 | 패키지 버킷(G1/G2/G3…)에 소속 |

> **핵심 개념 — "버킷(Bucket)":** 물품이 존재할 수 있는 자리를 하나의 축으로 통합한다.
> 물리 위치(한국 오피스, 김포 창고 A/B/C구역)와 투어 패키지(G1, G2, G3…)를 모두 "버킷"으로 취급한다.
> 한 물품 수량은 언제나 특정 버킷에 귀속되며, 상태 변경은 버킷 간 이동/추가/제거로 표현된다.

### 2-2. 물품 상태(이벤트) 3종

| 상태 | 의미 | 원장 기록 방식 |
|------|------|----------------|
| **구매** | 회사 자산으로 신규 추가 (①→②) | `출발버킷` = 빈칸, `도착버킷` = 지정 위치, 수량 = +N |
| **이동** | 회사 자산 내 위치/투어 배정 변경 (②↔③ 또는 ② 내 위치 이동) | `출발버킷` = 기존 버킷, `도착버킷` = 새 버킷, 수량 = N |
| **폐기** | 회사 자산에서 제거 (②/③→①), 고장 등 | `출발버킷` = 기존 버킷, `도착버킷` = 빈칸, 수량 = N |

### 2-3. 마스터 데이터 예시 (Settings 시트에서 관리, 자유롭게 추가/수정 가능해야 함)

- **위치(물리):** 한국 오피스, 김포 창고 A구역, 김포 창고 B구역, 김포 창고 C구역
- **투어 패키지:** G1, G2, G3 …
- **물품 카테고리:** Headset, Server, Floor, Misc …
- **물품명(카테고리 매핑):** VR 헤드셋(Headset), 서버(Server), AP(Floor), 케이블(Misc) …
- **상태:** 구매, 이동, 폐기

---

## 3. 시트 구성 (총 4개 시트)

### 3-1. `Settings` — 마스터 데이터

모든 목록의 단일 출처. 각 목록은 열 단위로 관리하고 **named range**로 노출한다.

| 열 | 내용 | Named Range 예시 |
|----|------|------------------|
| A | 물품 카테고리 | `LIST_CATEGORY` |
| B | 물품명 | (아래 표와 연동) |
| C | 물품명의 카테고리 | — |
| E | 위치(물리) | (버킷의 일부) |
| F | 투어 패키지 | (버킷의 일부) |
| G | 상태 | `LIST_STATUS` |
| I | **버킷 전체 목록** (위치 + 패키지 통합) | `LIST_BUCKET` |

- B·C열은 `물품명 ↔ 카테고리` 매핑 테이블. (예: `VR 헤드셋 | Headset`)
- I열 `LIST_BUCKET`은 E열(위치) + F열(패키지)을 합친 목록. 보기 시트의 열 머리글과 편집 드롭다운의 근거가 된다.
- 목록에 항목을 추가하면 드롭다운·집계가 자동 확장되도록 named range를 **여유 범위 또는 동적 범위**로 설정한다.

### 3-2. `Ledger` — 거래원장 (모든 편집이 append 되는 곳)

편집의 유일한 기록처. 사람이 직접 손대지 않고 스크립트만 기록한다(수동 편집도 가능하되 권장 X).

| 열 | 필드 | 설명 |
|----|------|------|
| A | 타임스탬프 | 기록 시각 (스크립트가 자동 입력) |
| B | 상태 | 구매 / 이동 / 폐기 |
| C | 카테고리 | 물품 카테고리 |
| D | 물품명 | |
| E | 출발버킷 | 구매 시 빈칸 |
| F | 도착버킷 | 폐기 시 빈칸 |
| G | 수량 | 양의 정수 |
| H | 메모 | 선택 (예: 폐기 사유) |

### 3-3. `재고현황` (View) — 함수 전용, 편집 금지

전체 물품을 한눈에 보는 매트릭스. **모든 수량 셀은 SUMIFS 수식**이다.

- 세로(행): 물품명 목록 (Settings에서 참조)
- 가로(열): 버킷 목록 `LIST_BUCKET` (한국 오피스 | 김포 A | 김포 B | 김포 C | G1 | G2 | G3 …)
- 각 셀 = `해당 물품이 그 버킷으로 들어온 합계(도착) − 나간 합계(출발)`
  - 수식 예: `=SUMIFS(Ledger!$G:$G, Ledger!$D:$D, $물품명, Ledger!$F:$F, 버킷) - SUMIFS(Ledger!$G:$G, Ledger!$D:$D, $물품명, Ledger!$E:$E, 버킷)`
- 맨 오른쪽에 **행 합계(= 회사 자산 총 보유 수량)** 열, 맨 아래에 **버킷별 합계** 행 추가.
- 조건부 서식: 수량 0은 흐리게, 음수는 빨간색(데이터 오류 감지용).

### 3-4. `편집` (Editor) — 사용자 입력 + 버튼

사용자가 실제로 조작하는 시트.

**입력 영역:**
1. **카테고리 선택** 셀 — 드롭다운(`LIST_CATEGORY` 참조).
2. **물품명 선택** 셀 — 위에서 고른 카테고리에 속한 물품만 보이는 **종속(dependent) 드롭다운**.
   - Apps Script `onEdit` 트리거로 카테고리 변경 시 물품 드롭다운 목록을 동적으로 갱신한다.
3. **현재 재고 표시 영역** — 물품을 고르면 그 물품의 **모든 버킷과 버킷별 현재 수량**을 자동 표시(함수 또는 onEdit로 채움). 읽기 전용.
4. **편집 입력 영역** — 사용자가 다음을 입력:
   - 상태 드롭다운(구매/이동/폐기, `LIST_STATUS`)
   - 출발버킷 드롭다운(`LIST_BUCKET`) — 구매일 땐 비활성/무시
   - 도착버킷 드롭다운(`LIST_BUCKET`) — 폐기일 땐 비활성/무시
   - 수량(양의 정수)
   - 메모(선택)

**버튼:**
- 시트에 **그림/도형을 버튼으로 삽입**하고 Apps Script 함수 `submitTransaction`을 할당한다.
- 버튼 클릭 시:
  1. 입력값 검증 (아래 4장 규칙).
  2. `Ledger` 시트에 거래 1줄 append (타임스탬프 자동).
  3. 성공 시 토스트/알림 표시(예: `✅ 반영 완료`), 입력 영역 초기화, 현재 재고 표시 갱신.
  4. 실패 시 사유를 알림(예: `❌ 재고 부족: 김포 창고 A구역에 케이블 3개뿐`).

---

## 4. 편집 검증 규칙 (submitTransaction 내부)

- 공통: 물품명·상태·수량 필수. 수량은 1 이상 정수.
- **구매:** 도착버킷 필수. 출발버킷 무시.
- **이동:** 출발버킷·도착버킷 둘 다 필수이며 서로 달라야 함. **출발버킷의 현재 수량 ≥ 이동 수량** 확인(음수 재고 방지).
- **폐기:** 출발버킷 필수. **출발버킷 현재 수량 ≥ 폐기 수량** 확인. 메모(사유) 권장.
- 현재 수량 계산은 `재고현황` 시트 값 또는 원장 재집계로 조회한다.
- 모든 검증 실패는 `SpreadsheetApp.getUi().alert()` 또는 toast로 명확한 한국어 메시지를 준다.

---

## 5. Apps Script 요구사항

- 함수: `submitTransaction()`(버튼), `onEdit(e)`(종속 드롭다운·현재재고 표시), `setupSheets()`(최초 시트/named range/검증 규칙 자동 생성), 필요한 헬퍼.
- 마스터 데이터·시트명·열 인덱스는 상단 상수로 관리(예: `const SHEET_LEDGER = 'Ledger'`).
- 모든 데이터 접근은 Settings named range 기반. 값 하드코딩 금지.
- 예외 처리와 한국어 사용자 메시지 포함.
- 이모지 스타일 메시지 사용: `✅` 성공, `❌` 오류, `⛔` 권한/차단.

---

## 6. 산출물 형식

1. **시트별 설계표** — 각 시트의 열·수식·데이터 검증·named range 정리.
2. **전체 Apps Script 코드** — 주석 포함, 복붙하면 바로 동작.
3. **셋업 가이드** — 새 스프레드시트에서 `setupSheets()` 실행 → 버튼에 함수 할당 → Settings 값 입력 순서, 스크린샷 없이 글로 단계별 설명.
4. **동작 시나리오 예시 3개** — (구매) VR 헤드셋 5개 김포 A 입고 / (이동) 김포 A → G1으로 2개 투어 투입 / (폐기) G1에서 고장 1개 폐기, 각 단계 후 `재고현황`이 어떻게 바뀌는지.

먼저 위 설계에서 모호하거나 개선이 필요한 점이 있으면 짧게 짚어준 뒤, 구현을 시작해 주세요.
